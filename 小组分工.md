# 数据与算法大作业五人小组分工方案

## 一、项目目标

本项目需要完成 KDD Cup 2017 Highway Tollgates Traffic Flow Prediction 风格的高速公路收费站交通流量预测任务。小组最终需要提交完整的预测结果、实验代码、模型说明、结果分析和课程汇报材料。

核心任务包括：

- 对 2016-09-19 至 2016-10-17 的训练数据进行清洗、聚合和特征构造。
- 利用 06:00-08:00、15:00-17:00 的先行流量信息，预测 2016-10-18 至 2016-10-24 每天 08:00-10:00、17:00-19:00 的车流量。
- 以 20 分钟为时间窗口，预测 5 个“收费站-方向”组合的 `volume`。
- 使用 MAPE 作为核心评价指标，重点降低小流量窗口和异常点对结果的影响。
- 最终以 XGBoost 为主模型，并对比历史均值、滑动均值、Linear/Ridge、RandomForest、GBDT 等基线方法。

## 二、总体流程

```text
原始数据理解
    ↓
数据清洗与时间窗口聚合
    ↓
滑动窗口样本构造
    ↓
时间、历史流量、天气、拓扑特征工程
    ↓
基线模型与 XGBoost 模型训练
    ↓
时间切分验证与 MAPE 分析
    ↓
生成最终提交文件
    ↓
撰写报告、制作汇报材料并分别展示各自工作
```

## 三、五人具体分工

### 成员一：项目负责人、数据理解与进度统筹

主要职责：

- 负责整体项目计划、任务拆解、进度协调和最终质量把关。
- 阅读作业要求，明确预测对象、预测时段、提交格式、评价指标和数据使用限制。
- 整理所有原始数据字段含义，包括收费站通行数据、车辆轨迹数据、天气数据、道路 link 和 route 数据。
- 维护项目目录结构、代码命名规范和文档规范。
- 组织小组会议，记录每次会议结论、当前问题和下一步安排。
- 检查各成员代码是否存在未来信息泄露，确保训练、验证、预测流程符合“只能使用预测窗口之前数据”的要求。

交付物：

- `README.md` 或项目说明文档。
- 数据字段说明表。
- 项目进度表。
- 最终代码和报告的整合版本。

建议关注点：

- 明确 5 个收费站-方向组合。
- 明确上午和下午预测窗口数量。
- 检查最终提交文件是否覆盖所有日期、时间窗口和收费站方向组合。

### 成员二：数据清洗、异常值处理与时间窗口聚合

主要职责：

- 负责读取原始收费站通行数据，并按 20 分钟窗口聚合 `volume`。
- 构造标准时间序列索引，补齐每个收费站-方向组合在训练期和测试先行期内的所有 20 分钟窗口。
- 处理缺失值，包括无车流窗口补 0、设备异常造成的缺失使用前向填充、后向填充或同时间段历史中位数填充。
- 进行异常值检测和修正，例如使用箱线图法、3σ 原则、分组中位数约束或邻域平滑。
- 分析异常点对 MAPE 的影响，保留异常处理前后的对比结果。
- 输出干净、统一、可供后续特征工程直接使用的流量时间序列表。

交付物：

- `data_cleaning.py` 或同等功能脚本。
- 20 分钟窗口聚合后的流量表。
- 缺失值处理说明。
- 异常值处理说明与可视化图表。

建议关注点：

- 缺失值处理必须保持时间顺序，不能使用未来预测期真实高峰数据。
- 对低流量窗口要谨慎处理，因为 MAPE 对真实值较小时非常敏感。
- 聚合后的字段建议包括 `tollgate_id`、`direction`、`window_start`、`window_end`、`volume`。

### 成员三：特征工程与滑动窗口样本构造

主要职责：

- 基于清洗后的 20 分钟流量表构造监督学习样本。
- 使用前 6 个连续窗口，也就是过去 2 小时流量，预测后续目标窗口。
- 构造历史流量特征，包括 `lag_1` 至 `lag_6`、前 1 小时均值、前 2 小时均值、最大值、最小值、标准差、变化率等。
- 构造先行流量特征，包括每天 06:00-08:00 对 08:00-10:00 的预测支持，以及 15:00-17:00 对 17:00-19:00 的预测支持。
- 构造时间特征，包括小时、分钟窗口编号、星期几、是否周末、是否早高峰、是否晚高峰、日期编号等。
- 融合天气特征，包括天气类型、温度、湿度、气压、风速、降水量、能见度等。
- 融合拓扑与路线特征，包括收费站 ID、方向、route 聚合长度、平均宽度、总车道数等。

交付物：

- `feature_engineering.py` 或同等功能脚本。
- 训练特征表。
- 验证特征表。
- 测试预测特征表。
- 特征列表与特征含义说明。

建议关注点：

- 所有 lag 特征必须只来自当前预测窗口之前。
- 时间切分验证时，验证集特征构造也要模拟真实预测过程。
- 类别变量可以使用 one-hot 编码、label encoding，或由模型直接处理。

### 成员四：模型训练、调参与融合

主要职责：

- 负责建立模型训练流程，并完成不同模型的对比实验。
- 先实现历史均值、滑动均值等简单基线模型。
- 实现 Linear Regression、Ridge、RandomForest、GBDT 等传统模型作为对照。
- 重点实现 XGBoost 回归模型，作为最终主模型。
- 根据时间切分验证集调节核心参数，例如 `n_estimators`、`max_depth`、`learning_rate`、`subsample`、`colsample_bytree`、`reg_alpha`、`reg_lambda`。
- 比较统一模型和分收费站-方向单独建模两种方式。
- 尝试简单加权融合，例如 XGBoost 与 GBDT、RandomForest 的加权平均，但控制复杂度。
- 输出最终预测结果，并确保 `volume` 非负。

交付物：

- `train_model.py` 或同等功能脚本。
- 各模型验证集 MAPE 对比表。
- 最优模型参数记录。
- 特征重要性图。
- 最终预测结果文件。

建议关注点：

- 评价指标使用 MAPE，而不是只看 MAE 或 RMSE。
- 训练集和验证集必须按时间切分，不能随机打乱。
- 可优先训练一个统一 XGBoost 模型，再根据效果决定是否拆分 5 个组合单独建模。

### 成员五：验证评估、结果分析与报告整合

主要职责：

- 负责设计验证方案，模拟真实预测流程评估模型效果。
- 编写 MAPE 计算函数，并按整体、日期、收费站、方向、早晚高峰分别统计误差。
- 检查最终提交文件格式是否正确，包括字段顺序、时间窗口格式、缺失预测行和异常负值。
- 分析误差较大的时间窗口，判断原因可能来自异常流量、天气变化、周末效应或模型欠拟合。
- 整理实验结果表格和可视化图表。
- 统筹课程报告的整合与格式统一，各成员需要提交自己负责部分的文字说明、图表和实验结果。
- 汇总答辩 PPT 的最终版本，各成员分别准备并讲解自己负责的项目内容。

交付物：

- `evaluate.py` 或同等功能脚本。
- MAPE 评估结果表。
- 误差分析图。
- 最终课程报告。
- 汇总后的答辩 PPT。
- 小组成员贡献说明。

建议关注点：

- 报告中要说明为什么 XGBoost 适合该任务。
- 对比实验要体现从基线到主模型的改进过程。
- 成员五只负责汇总和衔接，不单独承担全组答辩；每位成员都要能讲清楚自己负责的模块。

## 四、协作时间安排

| 阶段 | 时间建议 | 主要任务 | 负责人 |
| --- | --- | --- | --- |
| 第 1 阶段 | 第 1 天 | 理解题目、确认数据字段、制定目录结构 | 成员一 |
| 第 2 阶段 | 第 2-3 天 | 数据清洗、20 分钟窗口聚合、缺失值和异常值处理 | 成员二 |
| 第 3 阶段 | 第 3-4 天 | 滑动窗口样本构造、时间/天气/拓扑特征融合 | 成员三 |
| 第 4 阶段 | 第 5-6 天 | 基线模型、XGBoost 模型、参数调优、模型融合 | 成员四 |
| 第 5 阶段 | 第 6-7 天 | 验证评估、误差分析、生成最终提交文件 | 成员五 |
| 第 6 阶段 | 第 7-8 天 | 各成员撰写自己负责部分，统一整合报告和 PPT，并分别演练讲解 | 全体成员 |

## 五、推荐项目目录结构

```text
数据与算法大作业/
├── data/
│   ├── raw/                 # 原始数据
│   ├── processed/           # 清洗和聚合后的数据
│   └── submission/          # 最终提交文件
├── src/
│   ├── data_cleaning.py
│   ├── feature_engineering.py
│   ├── train_model.py
│   ├── evaluate.py
│   └── utils.py
├── notebooks/
│   └── analysis.ipynb
├── figures/
│   ├── volume_trend.png
│   ├── feature_importance.png
│   └── error_analysis.png
├── report/
│   ├── 课程报告.md
│   └── 答辩PPT.pptx
├── README.md
└── 小组分工.md
```

## 六、代码协作规范

- 每位成员负责自己的脚本，但输入输出字段必须提前统一。
- 所有中间数据尽量保存为 CSV，便于检查和复现实验。
- 函数命名要表达清楚含义，例如 `build_time_windows`、`create_lag_features`、`calculate_mape`。
- 每次修改重要代码后，需要记录修改内容和实验结果。
- 最终模型训练脚本应能从处理后的数据开始一键运行，生成预测结果。
- 不在模型训练、验证和预测阶段使用未来高峰期真实流量数据。

## 七、最终验收清单

- [ ] 已完成原始数据字段说明。
- [ ] 已完成 20 分钟窗口流量聚合。
- [ ] 已完成缺失值处理。
- [ ] 已完成异常值处理或平滑。
- [ ] 已完成滑动窗口样本构造。
- [ ] 已完成历史流量、时间、天气、拓扑特征融合。
- [ ] 已完成历史均值或滑动均值基线。
- [ ] 已完成 Linear/Ridge 等线性模型对比。
- [ ] 已完成 XGBoost 主模型训练与调参。
- [ ] 已完成按时间切分的验证实验。
- [ ] 已完成 MAPE 结果统计。
- [ ] 已完成误差分析。
- [ ] 已生成最终提交文件。
- [ ] 已检查提交文件字段和时间窗口格式。
- [ ] 已完成课程报告。
- [ ] 已完成答辩 PPT，且每位成员都包含自己负责模块的讲解页。
- [ ] 每位成员都能清楚说明自己负责的部分。

## 八、答辩汇报分配

答辩不是某一个人的单独任务，而是每位成员围绕自己负责的项目内容进行汇报。成员五可以负责 PPT 汇总和前后衔接，但不替代其他成员讲解。

| 成员 | 汇报内容 | 建议展示材料 |
| --- | --- |
| 成员一 | 任务背景、评价指标、数据使用限制、整体流程、项目组织方式 | 任务流程图、预测窗口说明、评价指标公式 |
| 成员二 | 数据清洗、20 分钟窗口聚合、缺失值处理、异常值处理和平滑方法 | 聚合样例表、异常值处理前后对比图 |
| 成员三 | 滑动窗口样本构造、历史流量特征、时间特征、天气特征、拓扑特征融合 | 特征工程流程图、特征列表、样本构造示例 |
| 成员四 | 历史均值/滑动均值基线、Linear/Ridge、RandomForest/GBDT、XGBoost 主模型、调参和模型融合 | 模型对比表、参数表、特征重要性图 |
| 成员五 | 时间切分验证方案、MAPE 统计、误差分析、提交文件检查、最终结果总结 | MAPE 结果表、分组误差图、最终提交格式示例 |

每位成员汇报时都需要说明：

- 自己负责的输入数据来自哪里。
- 自己做了哪些处理或建模工作。
- 输出结果交给了下一位成员的哪个环节。
- 本环节如何避免未来信息泄露。
- 本环节对最终 MAPE 或提交结果有什么贡献。

## 九、分工原则总结

本分工方案按照完整建模流程拆分，每位成员都有独立任务和明确交付物，同时又能在数据、特征、模型、验证和报告之间形成连续衔接。只要严格按照时间顺序构造特征，避免未来信息泄露，并以 XGBoost 为核心模型完成对比实验，就可以较完整地满足课程作业要求。
