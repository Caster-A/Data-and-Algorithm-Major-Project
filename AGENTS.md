# 数据与算法课程作业说明

## 作业主题

本次课程作业参考 KDD Cup 2017，由 Alibaba Cloud 承办，题目为 **Highway Tollgates Traffic Flow Prediction**。任务场景是高速公路收费站交通流量预测，用已有的交通、路网、时间、天气等数据预测未来高峰时段各收费站方向组合的车流量。

本质上，这是一个**时序回归预测问题**，但课程提示不建议把它当作长序列深度学习路线处理，而应采用：

- 数据处理
- 滑动窗口构造监督学习样本
- 特征工程
- 集成学习回归模型
- 特征融合后预测

## 预测任务

需要预测 **2016-10-18 至 2016-10-24** 指定高峰时段内的交通流量。

预测时段：

- 上午：08:00-10:00
- 下午：17:00-19:00

预测粒度：

- 每 **20 分钟** 一个时间窗口。

预测对象：

- 3 个收费站。
- 共 5 个“收费站-方向”组合。
- 收费站 2 只允许车辆驶入高速；其他收费站既有驶入也有驶出。

提交字段：

- `tollgate_id`：收费站 ID。
- `time_window`：时间窗口，例如 `[2016-09-18 08:40:00,2016-09-18 09:00:00)`。
- `direction`：方向，`0` 表示 entry，`1` 表示 exit。
- `volume`：预测总车流量。

## 数据范围与使用限制

初始训练集：

- 包含 **2016-09-19 至 2016-10-17** 期间采集的数据。

预测期：

- 对 **2016-10-18 至 2016-10-24** 的高峰时段进行预测。

测试/可用先验：

- 测试数据中会提供绿色时间段的交通数据，即每天：
  - 06:00-08:00
  - 15:00-17:00
- 可以把这些信息作为预测接下来两个小时高峰期的先行信息。

重要限制：

- 每次预测只能使用所预测时间窗口之前的数据。
- 不能使用未来信息，例如不能用 10 月 20 日的数据预测 10 月 19 日。

## 评价指标

评价指标为 **MAPE**（Mean Absolute Percentage Error，平均绝对百分比误差）：

```text
MAPE = average(|f_ct - p_ct| / f_ct)
```

其中：

- `c` 表示收费站-方向组合。
- `t` 表示测试期间的时间窗口。
- `f_ct` 表示真实交通流量。
- `p_ct` 表示预测交通流量。

注意：

- MAPE 越小越好。
- 当真实流量较小时，异常值和噪声会被放大，因此异常值处理很重要。

## 数据描述

项目包含静态与动态数据。

静态数据：

- 道路网络拓扑结构。
- 道路链路 `link` 信息。
- 路线 `route` 信息。
- 一条路线可表示为按顺序连接的一串道路链路。
- 每条链路包含长度、宽度、车道数、进入链路、流出链路、车道宽度等信息。

动态数据：

- 车辆轨迹数据。
- 收费站通行数据。
- 天气数据。

车辆轨迹数据字段包括：

- `intersection_id`
- `tollgate_id`
- `vehicle_id`
- `starting_time`
- `travel_seq`
- `travel_time`

其中 `travel_time` 表示车辆从路口到收费站总共花费的时间。

## 数据处理要求

### 异常值清洗与平滑

目的：

- 去除不合理记录。
- 降低噪声对模型的干扰。

常见异常：

- 极大或极小流量值。
- 采集错误导致的突变。
- 与前后时间窗口差异过大的点。

可用方法：

- 阈值删除。
- 3σ 原则。
- 箱线图法。
- 邻域平滑。

### 缺失值处理

缺失可能原因：

- 某时间片无记录。
- 采集设备故障。
- 数据同步问题。
- 天气数据时间粒度不同。

可用方法：

- 前向填充。
- 后向填充。
- 均值或中位数填充。
- 线性插值。
- 严重缺失样本删除。

注意：

- 缺失值处理要保持时间顺序。
- 不能引入未来信息。

## 滑动窗口样本构造

目的：

- 将时间序列预测问题转换为监督学习回归问题。

推荐构造：

- 输入：前 6 个连续 20 分钟窗口，即过去 2 小时。
- 输出：后 1 个或多个未来 20 分钟窗口。

示例：

```text
[a,b,c,d,e,f] -> g
[b,c,d,e,f,g] -> h
[c,d,e,f,g,h] -> i
```

意义：

- 用历史模式预测未来流量。
- 与课程图片中的“用前 2 小时预测后 2 小时”一致。

## 特征工程

可构造以下特征：

### 历史流量特征

- 前 6 个 20 分钟窗口的流量。
- 前 1 小时、前 2 小时统计量。
- 同一收费站-方向组合的历史均值、最大值、最小值、标准差。
- 06:00-08:00 或 15:00-17:00 的先行流量。

### 时间特征

- 日期。
- 小时。
- 分钟窗口编号。
- 星期几。
- 是否周末。
- 是否早高峰或晚高峰。
- 是否预测期内的某一天。

### 天气特征

- 天气类型。
- 温度。
- 气压。
- 湿度。
- 风速。
- 降水量。
- 能见度等。

### 拓扑与路线特征

- 收费站 ID。
- 方向。
- 路口 ID。
- route 相关信息。
- link 长度、宽度、车道数。
- route 总长度、平均宽度、总车道数等聚合特征。

## 特征融合方式

可采用简单直接的特征拼接：

- 历史流量特征。
- 时间特征。
- 天气特征。
- 拓扑特征。

也可采用加权融合：

- 短期流量特征权重大。
- 天气特征权重中等。
- 弱相关特征权重较小。

课程参考流程：

```text
数据处理 -> 特征提取 -> 特征融合 -> 预测
```

## 提到的候选算法

课程图片中提到的特征提取/回归模型包括：

- LASSO
- Ridge
- Linear Regression
- RandomForest
- ExtraTrees
- AdaBoost
- GBDT
- XGBoost

特征融合阶段提到：

- GBDT
- XGBoost
- XGBoost2

## 推荐最佳算法：XGBoost

本作业优先选择 **XGBoost 回归模型** 作为最佳算法。

选择理由：

- 它是 GBDT 的强化版本，适合结构化表格数据。
- 能学习历史流量、时间、天气、拓扑特征与未来流量之间的非线性关系。
- 加入正则化，泛化能力通常优于普通 GBDT。
- 训练效率高，适合快速调参和多组收费站-方向建模。
- 支持缺失值处理，对真实交通数据较友好。
- 课程图片中明确强调 XGBoost 可作为核心回归模型之一，用于预测未来 traffic volume。

建议实现方式：

- 将每个 20 分钟窗口聚合为一行样本。
- 以 `tollgate_id + direction + time_window` 为预测粒度。
- 用前 6 个窗口流量、时间、天气、拓扑等特征预测未来窗口 `volume`。
- 可先训练一个统一的 XGBoost 模型，并把 `tollgate_id`、`direction` 作为类别特征编码。
- 如不同收费站方向差异较大，也可为 5 个收费站-方向组合分别训练模型。

推荐基线：

1. 先做历史均值/滑动均值基线。
2. 再做 Linear/Ridge 回归基线。
3. 最终使用 XGBoost 作为主模型。
4. 可额外尝试 GBDT、RandomForest、ExtraTrees 与 XGBoost 做加权融合，但不要让实现复杂度超过课程作业要求。

## 实验注意事项

- 严格按时间切分训练与验证集，避免数据泄露。
- 验证时模拟真实预测：只能使用预测窗口之前的数据。
- 对 MAPE 敏感的小流量窗口要重点检查异常值。
- 对缺失值、异常值、天气粒度不一致问题要先处理再建模。
- 输出结果应保证每个收费站-方向组合、每个目标时间窗口都有预测值。
